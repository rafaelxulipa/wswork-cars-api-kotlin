spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/wswork_cars_kt
    username: wswork
    password: wswork123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8081
